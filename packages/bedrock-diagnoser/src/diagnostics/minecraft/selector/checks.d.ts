import { CompactJson } from 'bc-minecraft-bedrock-types/src/minecraft/json';
import { Minecraft } from 'bc-minecraft-bedrock-types';
import { DiagnosticsBuilder } from '../../../types';
/**
 * Checking if the given attribute is the only one.
 * @param attrs The attributes to check
 * @param sel The selector
 * @param diagnoser The diagnoser
 * @returns True if the attribute is the only one, false otherwise
 */
export declare function selectorattributes_no_duplicate(attrs: CompactJson.IKeyNode[], sel: Minecraft.Selector.Selector, diagnoser: DiagnosticsBuilder): boolean;
//# sourceMappingURL=checks.d.ts.map