import { Types } from "bc-minecraft-bedrock-types";
import { DiagnosticsBuilder } from "../../types";
/**
 * Checks if the given name exists or is valid.
 * @param value The name to check
 * @param diagnoser The diagnoser
 * @returns True if the name exists
 */
export declare function minecraft_name_diagnose(value: Types.OffsetWord, diagnoser: DiagnosticsBuilder): boolean;
