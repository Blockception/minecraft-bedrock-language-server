import { Command } from 'bc-minecraft-bedrock-command';
/**
 * Tries to determine the possible type of the entity in a command
 * @param command The command to check
 * @param maxIndex The max index to check
 * @returns The possible type of the entity
 */
export declare function GetPossibleEntityTypes(command: Command, maxIndex: number): string[];
export declare function GetPossibleBlockID(command: Command, maxIndex: number): string | undefined;
//# sourceMappingURL=commands.d.ts.map