import { Diagnostic } from 'vscode-languageserver';
import { CodeActionBuilder } from '../builder';
/**
 * Adds a given type and value to the definition
 * @param builder
 * @param diag
 * @param type
 */
export declare function definition(builder: CodeActionBuilder, diag: Diagnostic, type: string): void;
/**Adds a given type and value to the definition
 * @param builder
 * @param diag
 * @param type
 */
export declare function attributes(builder: CodeActionBuilder, diag: Diagnostic): void;
//# sourceMappingURL=definition.d.ts.map